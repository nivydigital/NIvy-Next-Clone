# STD-04 — Tool & Side-Effect Standard

Safe read (no approval) vs protected write (explicit approval) vs forbidden ambient (deny).

Argument schemas, timeouts, mock mode, audit with redacted args, fail-closed policy.
