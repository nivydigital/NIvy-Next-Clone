# P0.3 — Runtime API discovery

**Date:** 2026-09-17

## Current routes (backend/app/main.py)

| Route | Purpose |
|-------|---------|
| GET `/health` | Service + runtime_engine.health |
| GET `/api/v1/system` | High-level system labels |
| GET `/api/v1/runtime/health` | Runtime health |
| POST `/api/v1/runtime/agents/{agent_id}/run` | Generic LLM run |
| POST `/api/v1/runtime/agents/A001/research` … A005/channel | Structured entrypoints only |
| POST `/api/v1/runtime/approvals` (+ approve) | Approval gate |
| POST `/api/v1/runtime/agents/{agent_id}/tools/{tool_id}/execute` | Tool execute |
| POST `/api/v1/email/send` | Email with approval |
| Revenue lead routes | CRM-lite |
| GET `/api/v1/evaluation/runtime` | Basic runtime checks |

## Discovery gap

There is **no** `GET /api/v1/runtime/agents` listing:
- agents from registry.yaml
- whether `backend/app/runtime/a0XX.py` exists
- whether a dedicated route exists
- registry status / runtime field

## Intended discovery response shape

```json
{
  "count": 0,
  "items": [
    {
      "id": "A034",
      "name": "Lead Discovery",
      "registry_status": "declared",
      "runtime_module": "backend/app/runtime/a034.py",
      "runtime_module_exists": true,
      "dedicated_route": null,
      "structured_entrypoint": "run_a034_*",
      "tools": ["tool.ollama.generate"],
      "prompts": ["PR003"],
      "skills": ["SK034", "SK035"]
    }
  ]
}
```

## Implementation status

- [x] Documented gap and target shape
- [ ] Code: add GET `/api/v1/runtime/agents` (scan registry + filesystem)
- [ ] Code: optional GET `/api/v1/runtime/agents/{id}` detail
- [ ] Wire structured dispatch for agents with `run_a0XX_*` (beyond A001–A005)

## Note

Full auto-import of 90+ modules into main.py is brittle. Prefer:
1. Discovery endpoint (filesystem + registry)
2. Dynamic import of `backend.app.runtime.a0XX` by agent_id in a single `/execute` handler
